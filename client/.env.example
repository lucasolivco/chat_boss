# A URL da API é resolvida dinamicamente pelo hostname — não precisa configurar em desenvolvimento.
# Descomente abaixo APENAS para forçar um endereço fixo (ex: produção):
# VITE_API_URL=http://localhost:3000
